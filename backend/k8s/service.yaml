apiVersion: v1
kind: Service
metadata:
  name: orbitops-api-service
  namespace: default
  labels:
    app: orbitops-api
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: orbitops-api
